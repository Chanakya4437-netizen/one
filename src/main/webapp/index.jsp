:root { --bg: #f5f6fa; --surface: #fff; --surface-2: #eef0f5; --text: #1a1d28; --muted: #767b8e; --primary: #5b4bff; --primary-light: #eeecff; --primary-dark: #4638db; --accent: #ff5c7a; --accent-light: #fff0f3; --success: #16a34a; --success-light: #ecfdf5; --border: #e4e6ed; --shadow-sm: 0 1px 3px rgba(18,20,32,.06); --shadow: 0 8px 28px rgba(18,20,32,.07); --shadow-lg: 0 18px 45px rgba(18,20,32,.09); --radius-sm: 10px; --radius: 16px; --radius-lg: 22px; --container: 1260px; --transition: .22s cubic-bezier(.4,0,.2,1); } * { box-sizing: border-box; margin: 0; padding: 0; } html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; } body { background: var(--bg); color: var(--text); font-family: Inter, system-ui, sans-serif; line-height: 1.55; overflow-x: hidden; } img { max-width: 100%; display: block; } a { text-decoration: none; color: inherit; } button, input, select { font: inherit; } button, a { cursor: pointer; } button:focus-visible, a:focus-visible, input:focus-visible, select:focus-visible { outline: 3px solid rgba(91,75,255,.35); outline-offset: 2px; } .container { width: min(var(--container), calc(100% - 44px)); margin: auto; } .muted { color: var(--muted); } .section { padding: 76px 0; } .section-head { display: flex; align-items: flex-end; justify-content: space-between; gap: 18px; margin-bottom: 32px; } .section-head h2 { font: 700 31px Poppins, sans-serif; letter-spacing: -.025em; color: var(--text); } .section-head p { margin-top: 6px; color: var(--muted); font-size: 15px; } .link { color: var(--primary); font-weight: 700; font-size: 14px; display: inline-flex; align-items: center; gap: 6px; transition: var(--transition); } .link:hover { gap: 10px; color: var(--primary-dark); } /* ── Topbar ── */ .topbar { background: #1a1d28; color: #d3d5dd; font-size: 12.5px; letter-spacing: .01em; } .topbar .container { min-height: 38px; display: flex; align-items: center; justify-content: space-between; gap: 16px; } .topbar i { margin-right: 5px; color: var(--primary-light); } /* ── Header ── */ header { position: sticky; top: 0; z-index: 100; background: rgba(255,255,255,.94); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border-bottom: 1px solid var(--border); transition: var(--transition); } .header-main { height: 78px; display: flex; align-items: center; gap: 28px; } .logo { font: 800 24px Poppins, sans-serif; white-space: nowrap; letter-spacing: -.02em; } .logo span { color: var(--primary); } /* Search */ .search { flex: 1; max-width: 640px; position: relative; } .search input { width: 100%; height: 48px; border: 2px solid var(--border); border-radius: 14px; background: var(--surface-2); padding: 0 50px 0 18px; outline: 0; font-size: 14px; transition: var(--transition); } .search input:focus { background: #fff; border-color: var(--primary); box-shadow: 0 0 0 5px rgba(91,75,255,.08); } .search button { position: absolute; right: 6px; top: 6px; width: 38px; height: 38px; border: 0; border-radius: 10px; background: var(--primary); color: #fff; font-size: 15px; transition: var(--transition); } .search button:hover { background: var(--primary-dark); transform: scale(1.04); } /* Header actions */ .header-actions { display: flex; align-items: center; gap: 4px; margin-left: auto; } .icon-action { position: relative; width: 44px; height: 44px; border: 0; background: transparent; border-radius: 12px; display: grid; place-items: center; font-size: 18px; color: #4a4e5c; transition: var(--transition); } .icon-action:hover { background: var(--surface-2); color: var(--primary); } .badge-count { position: absolute; right: 2px; top: 3px; background: var(--accent); color: #fff; border-radius: 999px; min-width: 19px; height: 19px; padding: 0 5px; font-size: 10px; font-weight: 800; display: grid; place-items: center; border: 2.5px solid #fff; transition: transform .2s; } .icon-action:hover .badge-count { transform: scale(1.12); } /* Nav */ .nav-row { border-top: 1px solid var(--border); background: #fff; } .nav { height: 50px; display: flex; align-items: center; gap: 30px; overflow-x: auto; white-space: nowrap; } .nav a { font-size: 14px; font-weight: 600; color: #5e6370; padding: 4px 0; border-bottom: 2.5px solid transparent; transition: var(--transition); } .nav a:hover, .nav a.active { color: var(--primary); border-bottom-color: var(--primary); } .menu-btn { display: none; } /* ── Hero ── */ .hero { padding: 30px 0 14px; } .hero-card { min-height: 480px; border-radius: var(--radius-lg); overflow: hidden; position: relative; background: linear-gradient(105deg, rgba(15,17,28,.9) 0%, rgba(15,17,28,.55) 48%, rgba(15,17,28,.1) 100%), url('[images.unsplash.com](https://images.unsplash.com/photo-1555529669-e69e7aa0ba9a?auto=format&fit=crop&w=1600&q=85)') center/cover; } .hero-content { max-width: 620px; padding: 76px 72px; color: #fff; } .eyebrow { display: inline-flex; align-items: center; gap: 7px; padding: 7px 13px; border-radius: 999px; background: rgba(255,255,255,.16); font-size: 12px; font-weight: 800; letter-spacing: .09em; text-transform: uppercase; } .hero h1 { font: 800 clamp(38px, 5vw, 62px)/1.05 Poppins, sans-serif; margin: 22px 0 15px; letter-spacing: -.04em; } .hero p { font-size: 17px; color: rgba(255,255,255,.84); max-width: 540px; line-height: 1.6; } .hero-actions { display: flex; gap: 13px; margin-top: 30px; flex-wrap: wrap; } /* ── Buttons ── */ .btn { border: 0; border-radius: 12px; padding: 13px 22px; font-weight: 700; font-size: 14px; display: inline-flex; align-items: center; justify-content: center; gap: 8px; transition: var(--transition); letter-spacing: .01em; } .btn:active { transform: scale(.97); } .btn-primary { background: var(--primary); color: #fff; } .btn-primary:hover { background: var(--primary-dark); transform: translateY(-2px); box-shadow: 0 6px 22px rgba(91,75,255,.3); } .btn-light { background: #fff; color: #1a1d28; } .btn-light:hover { transform: translateY(-2px); box-shadow: 0 6px 22px rgba(0,0,0,.12); } .btn-outline { background: transparent; border: 2px solid var(--border); color: var(--text); } .btn-outline:hover { border-color: var(--primary); color: var(--primary); background: var(--primary-light); } .btn-small { padding: 9px 14px; font-size: 13px; border-radius: 10px; } .btn-block { width: 100%; } /* ── Benefits ── */ .benefits { display: grid; grid-template-columns: repeat(4, 1fr); gap: 14px; margin-top: 14px; } .benefit { background: #fff; border: 1px solid var(--border); border-radius: var(--radius); padding: 18px 18px; display: flex; gap: 14px; align-items: center; transition: var(--transition); } .benefit:hover { transform: translateY(-2px); box-shadow: var(--shadow); border-color: #d8d3ff; } .benefit i { width: 44px; height: 44px; border-radius: 12px; background: var(--primary-light); color: var(--primary); display: grid; place-items: center; font-size: 18px; flex-shrink: 0; } .benefit strong { display: block; font-size: 14px; margin-bottom: 2px; } .benefit span { font-size: 12px; color: var(--muted); } /* ── Categories ── */ .categories { display: grid; grid-template-columns: repeat(6, 1fr); gap: 15px; } .category { border: 2px solid var(--border); background: #fff; border-radius: var(--radius); padding: 24px 14px; text-align: center; transition: var(--transition); font-family: inherit; } .category:hover { transform: translateY(-5px); box-shadow: var(--shadow-lg); border-color: #c8c2ff; } .category:active { transform: translateY(-2px); } .category i { font-size: 28px; color: var(--primary); margin-bottom: 12px; display: block; } .category strong { display: block; font-size: 14px; margin-bottom: 3px; } .category span { font-size: 12px; color: var(--muted); } /* ── Toolbar ── */ .toolbar { display: flex; align-items: center; justify-content: space-between; gap: 14px; margin-bottom: 22px; flex-wrap: wrap; } .filters { display: flex; gap: 8px; overflow-x: auto; padding-bottom: 2px; } .filter { border: 2px solid var(--border); background: #fff; padding: 9px 15px; border-radius: 999px; font-size: 13px; font-weight: 700; white-space: nowrap; transition: var(--transition); color: #5e6370; } .filter.active, .filter:hover { background: var(--primary-light); border-color: #c8c2ff; color: var(--primary); } .filter.active { box-shadow: 0 2px 10px rgba(91,75,255,.15); } .sort { border: 2px solid var(--border); background: #fff; border-radius: 10px; padding: 9px 14px; font-size: 13px; font-weight: 600; color: var(--text); cursor: pointer; transition: var(--transition); } .sort:focus { border-color: var(--primary); } /* ── Products ── */ .products { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; } .product { background: #fff; border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; position: relative; transition: var(--transition); display: flex; flex-direction: column; } .product:hover { transform: translateY(-6px); box-shadow: var(--shadow-lg); } .product-image { height: 240px; background: #f3f4f8; position: relative; overflow: hidden; } .product-image img { width: 100%; height: 100%; object-fit: cover; transition: transform .4s cubic-bezier(.4,0,.2,1); } .product:hover .product-image img { transform: scale(1.06); } .product-badge { position: absolute; left: 12px; top: 12px; background: #1a1d28; color: #fff; padding: 6px 10px; border-radius: 8px; font-size: 11px; font-weight: 800; z-index: 1; } .product-badge.sale { background: var(--accent); } .wish { position: absolute; right: 11px; top: 11px; width: 38px; height: 38px; border: 0; border-radius: 50%; background: #fff; box-shadow: 0 3px 12px rgba(0,0,0,.1); color: #6b6f7a; z-index: 2; display: grid; place-items: center; font-size: 15px; transition: var(--transition); } .wish:hover { transform: scale(1.1); } .wish.active { color: var(--accent); background: var(--accent-light); } .wish.active:hover { transform: scale(1.1); } .product-body { padding: 17px 17px 12px; flex: 1; } .product-category { font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: .09em; font-weight: 800; margin-bottom: 4px; } .product h3 { font-size: 15px; margin: 0 0 8px; font-weight: 600; } .rating { font-size: 12px; color: #f59e0b; margin-bottom: 4px; } .rating span { color: var(--muted); font-weight: 500; } .price-row { display: flex; align-items: flex-end; justify-content: space-between; margin-top: 10px; } .price { font-size: 19px; font-weight: 800; color: var(--text); } .old { font-size: 12px; color: var(--muted); text-decoration: line-through; margin-left: 6px; font-weight: 500; } .product-footer { padding: 0 17px 17px; display: flex; gap: 8px; } .add { flex: 1; border: 0; background: #1a1d28; color: #fff; border-radius: 11px; padding: 11px; font-weight: 700; font-size: 13px; transition: var(--transition); } .add:hover { background: var(--primary); } .add:active { transform: scale(.97); } .quick { width: 44px; border: 2px solid var(--border); background: #fff; border-radius: 11px; font-size: 15px; color: #5e6370; transition: var(--transition); } .quick:hover { border-color: var(--primary); color: var(--primary); background: var(--primary-light); } /* ── Empty state ── */ .empty-state { display: none; text-align: center; padding: 60px 20px; } .empty-state i { font-size: 52px; color: #d1d4de; margin-bottom: 16px; } .empty-state p { color: var(--muted); font-size: 15px; } /* ── Deal ── */ .deal { display: grid; grid-template-columns: 1.05fr .95fr; background: #1a1d28; border-radius: var(--radius-lg); overflow: hidden; color: #fff; } .deal-image { min-height: 410px; background: url('[images.unsplash.com](https://images.unsplash.com/photo-1517336714731-489689fd1ca8?auto=format&fit=crop&w=1200&q=85)') center/cover; } .deal-content { padding: 56px 56px; display: flex; flex-direction: column; justify-content: center; } .deal-label { color: #ff91a7; font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: .13em; } .deal h2 { font: 800 37px Poppins, sans-serif; margin: 10px 0; letter-spacing: -.03em; } .deal p { color: #bfc3cf; line-height: 1.65; } .timer { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; margin: 26px 0; } .time { background: #292d3a; border-radius: 14px; padding: 14px 10px; text-align: center; transition: var(--transition); } .time strong { display: block; font-size: 24px; font-weight: 800; } .time span { font-size: 10px; color: #aeb3c0; text-transform: uppercase; letter-spacing: .06em; } .deal-price { font-size: 31px; font-weight: 800; } .deal-price del { font-size: 15px; color: #9da2af; margin-left: 10px; font-weight: 500; } /* ── Testimonials ── */ .testimonials { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; } .review { background: #fff; border: 1px solid var(--border); border-radius: var(--radius); padding: 24px; transition: var(--transition); } .review:hover { box-shadow: var(--shadow); transform: translateY(-3px); } .review .rating { margin-bottom: 10px; } .review p { color: #4f5560; font-size: 14px; line-height: 1.65; } .review-user { display: flex; align-items: center; gap: 11px; margin-top: 20px; padding-top: 16px; border-top: 1px solid var(--border); } .avatar { width: 42px; height: 42px; border-radius: 50%; object-fit: cover; border: 2px solid var(--primary-light); } /* ── Newsletter ── */ .newsletter { background: linear-gradient(135deg, #5b4bff, #7d6fff); border-radius: var(--radius-lg); color: #fff; padding: 52px 52px; display: flex; align-items: center; justify-content: space-between; gap: 34px; position: relative; overflow: hidden; } .newsletter::after { content: ''; position: absolute; right: -50px; bottom: -50px; width: 200px; height: 200px; background: rgba(255,255,255,.06); border-radius: 50%; } .newsletter > * { position: relative; z-index: 1; } .newsletter h2 { font: 800 30px Poppins, sans-serif; margin: 0 0 8px; } .newsletter p { margin: 0; color: #e9e7ff; font-size: 15px; } .news-form { display: flex; gap: 10px; min-width: min(480px, 100%); } .news-form input { flex: 1; border: 0; border-radius: 12px; padding: 14px 17px; outline: 0; font-size: 14px; } .news-form .btn { white-space: nowrap; } /* ── Footer ── */ footer { background: #1a1d28; color: #fff; padding: 58px 0 28px; } .footer-grid { display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr; gap: 38px; } footer .logo { display: inline-block; margin-bottom: 12px; } footer h4 { margin: 0 0 14px; font-size: 15px; font-weight: 700; } footer p, footer a { color: #aeb3c0; font-size: 13.5px; } footer a { display: block; margin: 9px 0; transition: var(--transition); } footer a:hover { color: #fff; padding-left: 4px; } .socials { display: flex; gap: 9px; margin-top: 20px; } .socials a { width: 40px; height: 40px; border: 1px solid #333846; border-radius: 11px; display: grid; place-items: center; margin: 0; color: #fff; transition: var(--transition); } .socials a:hover { background: var(--primary); border-color: var(--primary); padding-left: 0; } .copyright { border-top: 1px solid #2b2f3b; margin-top: 38px; padding-top: 22px; text-align: center; color: #8e94a2; font-size: 12.5px; } /* ── Drawer / Cart ── */ .overlay { position: fixed; inset: 0; background: rgba(0,0,0,.45); z-index: 150; display: none; backdrop-filter: blur(2px); } .overlay.show { display: block; } .drawer { position: fixed; right: 0; top: 0; height: 100%; width: min(440px, 94vw); background: #fff; z-index: 160; transform: translateX(100%); transition: transform .28s cubic-bezier(.4,0,.2,1); padding: 24px; display: flex; flex-direction: column; box-shadow: -8px 0 40px rgba(0,0,0,.12); } .drawer.open { transform: translateX(0); } .drawer-head { display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--border); padding-bottom: 17px; } .drawer-head strong { font-size: 20px; } .drawer-items { flex: 1; overflow-y: auto; padding: 8px 0; } .cart-item { display: flex; gap: 13px; padding: 15px 0; border-bottom: 1px solid var(--border); align-items: center; } .cart-item img { width: 70px; height: 70px; border-radius: 12px; object-fit: cover; } .cart-item h4 { margin: 0; font-size: 14px; } .cart-item p { margin: 3px 0; font-size: 13px; color: var(--muted); } .cart-item .link { font-size: 12px; color: var(--accent); } .drawer-total { border-top: 1px solid var(--border); padding-top: 18px; } .total-row { display: flex; justify-content: space-between; font-weight: 800; margin-bottom: 16px; font-size: 17px; } /* ── Toast ── */ .toast { position: fixed; bottom: 28px; left: 50%; transform: translate(-50%, 20px); background: #1a1d28; color: #fff; padding: 13px 20px; border-radius: 12px; z-index: 300; opacity: 0; pointer-events: none; transition: all .28s cubic-bezier(.4,0,.2,1); font-size: 13.5px; font-weight: 600; box-shadow: 0 8px 30px rgba(0,0,0,.2); } .toast.show { opacity: 1; transform: translate(-50%, 0); } /* ── Responsive ── */ @media (max-width: 1050px) { .categories { grid-template-columns: repeat(3, 1fr); } .products { grid-template-columns: repeat(3, 1fr); } .benefits { grid-template-columns: repeat(2, 1fr); } .hero-content { padding: 64px 48px; } } @media (max-width: 800px) { .container { width: min(var(--container), calc(100% - 28px)); } .topbar { display: none; } .header-main { height: 68px; } .search { order: 3; position: absolute; left: 14px; right: 14px; top: 74px; max-width: none; display: none; } .search.mobile-open { display: block; } .header-main { position: relative; } .menu-btn { display: grid; width: 42px; height: 42px; border: 0; background: var(--surface-2); border-radius: 11px; place-items: center; font-size: 17px; } .nav-row { display: none; } .logo { font-size: 21px; } .header-actions { gap: 0; } .hero { padding-top: 16px; } .hero-card { min-height: 520px; } .hero-content { padding: 52px 28px; } .hero h1 { font-size: 40px; } .products { grid-template-columns: repeat(2, 1fr); } .deal { grid-template-columns: 1fr; } .deal-image { min-height: 260px; } .deal-content { padding: 38px; } .testimonials { grid-template-columns: 1fr; } .newsletter { padding: 38px; flex-direction: column; text-align: center; } .news-form { margin-top: 20px; min-width: 0; } .footer-grid { grid-template-columns: 1fr 1fr; } } @media (max-width: 560px) { .section { padding: 50px 0; } .section-head { flex-direction: column; align-items: flex-start; } .section-head .link { margin-top: 8px; } .benefits { grid-template-columns: 1fr 1fr; } .categories { grid-template-columns: repeat(2, 1fr); } .products { grid-template-columns: 1fr; } .hero-card { min-height: 540px; } .hero h1 { font-size: 34px; } .timer { gap: 5px; } .time { padding: 10px 4px; } .time strong { font-size: 19px; } .deal h2 { font-size: 28px; } .deal-content { padding: 30px 24px; } .footer-grid { grid-template-columns: 1fr; } .newsletter h2 { font-size: 24px; } .newsletter { padding: 30px 24px; } .news-form { flex-direction: column; } .hero-actions { flex-direction: column; } .hero-actions .btn { width: 100%; justify-content: center; } }
Free shipping on orders over $50 24/7 support · Easy returns
